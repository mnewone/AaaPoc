package jms;

public class Receiver {
/*
	  private final static Logger LOGGER = LoggerFactory.getLogger(Receiver.class);
	  
	  //@JmsListener(containerFactory = "myJmsListenerContainerFactory",destination = "start")
	  public void receive(MapMessage message) throws JMSException{
		  LOGGER.info("received message: " + message.getString("jobOrder") + message.getString("tableName"));
		  System.out.println("oldMessage received!" + message.toString());
	  }
	  */
}
